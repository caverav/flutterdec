use flutterdec_adapter::FunctionInfo;
use crate::snapshot::DataSnapshot;

pub fn produce_functions_info(snapshot: &DataSnapshot) -> anyhow::Result<Vec<FunctionInfo>> {
    let functions_info = Vec::new();

    Ok(functions_info)
}
