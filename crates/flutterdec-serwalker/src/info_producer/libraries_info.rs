use crate::snapshot::DataSnapshot;
use flutterdec_adapter::LibraryInfo;

pub fn produce_libraries_info(snapshot: &DataSnapshot) -> anyhow::Result<Vec<LibraryInfo>> {
    let libraries_info = Vec::new();

    Ok(libraries_info)
}
